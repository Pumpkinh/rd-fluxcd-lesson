apiVersion: v1
kind: Service
metadata:
  name: course-app
  labels:
    app.kubernetes.io/name: course-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: course-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
